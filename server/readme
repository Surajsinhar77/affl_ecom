I have Create the folder Structure for the Backened u need to write Everythings on it
    1. Firstly Create the Route for instance 
        then Write routes in Router file and exports 
    2. Then you need to create the Model for model for perticullar file for every diffrent routes type 
        and connect with controller
    3. Controller folder for the api , write Api into the Controller file 
        create file in controller folder with some name and writes yours api into it and then exports it.

    4. We Need one one more things knows as middleware 
        in which we need to write api for middleware which can cheack or authrorization and authentication for the apis.